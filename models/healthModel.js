class health {
    constructor(id, registration_date, weight, heart_rate, allergy) {
        (this.id = id),
        (this.registration_date = registration_date),
        (this.weight = weight),
        (this.heart_rate = heart_rate),
        (this.allergy = allergy);
    }
}

export default health;