class integration {
    constructor(id, token, date_expire, ) {
        (this.id = id),
        (this.token = token),
        (this.date_expire = date_expire);
        
        
    }
}

export default integration;