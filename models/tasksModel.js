class task {
    constructor(id, title, description, expiration_date,state ) {
        (this.id = id),
        (this.title = title),
        (this.description = description),
        (this.expiration_date= expiration_date),
        (this.state = state);
        
    }
}

export default task;