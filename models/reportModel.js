class report {
    constructor(id, name, generation_date, description, format){
        (this.id = id),
        (this.name = name),
        (this.generation_date = generation_date),
        (this.description = description),
        (this.format = format);
    }
}
export default report;