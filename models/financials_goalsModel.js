class financials_goals{
    constructor(id, description, target_amount, current_amount, deadline ) {
        (this.id = id),
        (this.description = description),
        (this.target_amount = target_amount),
        (this.current_amount = current_amount),
        (this.deadline = deadline);
        
    }
}

export default financials_goals;